#include <stdio.h>

/**
 * main - print only single numbers of base ten
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int a;
for (a = 0; a < 10; a++)
printf("%d", a);
printf("\n");
return (0);
}
