#include "main.h"

/**
 * mul - it multiplies two intergers
 * @a: - it is the first number to be multiplied
 * @b: - second number to be multiplied
 * Return: the value after multiplication
*/

int mul(int a, int b)
{
return (a * b);
}
