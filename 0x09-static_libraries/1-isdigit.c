#include "main.h"
		
/**
 * _isdigit - used in checking num from zero to nine
 * @c: - the parameter interger being checked
 * Return: 1 if c a num, 0 otherwise
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
