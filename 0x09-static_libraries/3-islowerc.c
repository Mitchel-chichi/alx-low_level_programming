#include "main.h"
/**
 * _islower - used for looking for lowercase letters
 * @c:- the parameter to look
 * Return: 1 if c lowercase, 0 otherwise
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
