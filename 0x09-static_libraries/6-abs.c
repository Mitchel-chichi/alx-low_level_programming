#include "main.h"


/**
 * _abs - used for calculating absolute value of integer
 * @n: - the parameter being checked
 * Return: the value of parameter
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
