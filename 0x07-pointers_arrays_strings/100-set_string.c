#include "main.h"

/**
 * set_string - used for setting the pointers value to a character
 * @s: the parameter that is a double pointer
 * @to: - the parameter that is a pointer to character
 */

void set_string(char **s, char *to)
{
*s = to;
}
